# 🛡 Политика безопасности Seednox v1.0.1

> **Ваши секреты всегда под надёжной аметистовой защитой!**

---

## 🔗 Официальные ресурсы

- 📢 **Telegram-канал**: [@seednox](https://t.me/seednox)
- 🌐 **Официальный сайт**: [novirx.cyou/seednox/](https://novirx.cyou/seednox/)
- 🐙 **Исходный код на GitHub**: [github.com/novirx-tg/seednox](https://github.com/novirx-tg/seednox)

---

## 🔍 Сообщить об уязвимости

Обнаружили проблему безопасности? **Не создавайте публичный issue.**

Создайте приватный отчёт через [GitHub Security Advisories](https://github.com/novirx-tg/seednox/security/advisories/new).  
Мы ответим в течение 48 часов.

---

## 🏗 Архитектура безопасности v1.0.1

### Что мы НЕ храним
- Любые секреты (сид-фразы, пароли, ключи, заметки) в открытом виде.
- Мастер-пароли — ни в базе данных, ни в логах.
- Ключи шифрования на диске — ключи существуют исключительно в RAM во время открытой сессии.

### Что мы храним (зашифровано)

| Данные | Формат & Криптография |
|--------|-----------------------|
| **Все типы секретов** | `AES-256-GCM` (12-byte nonce + ciphertext + GCM tag) |
| **Тип записи** | Метаданные `entry_type` в открытом виде (только тип, не содержимое) |
| **Пароль верификации** | `Argon2id` (`t=3, m=64MB, p=4`) — только хэш |
| **Соль KDF** | 32 байта `os.urandom(32)` — уникальна для каждого пользователя |
| **Портативный бэкап** | Заголовок `SNX1` + embedded salt + `AES-256-GCM` |

### Криптографический стек

```
Мастер-пароль + Соль
        ↓
    Argon2id KDF (t=3, m=64MB, p=4)
        ↓
    AES-256-GCM ключ (256 бит)
        ↓
  Зашифрованная запись (сид, пароль, ключ, заметка...)
```

---

## 🛡 Защита от угроз

| Угроза | Мера защиты |
|--------|-------------|
| **Брутфорс пароля** | `Argon2id` KDF + блокировка после 5 попыток на 15 мин |
| **Перехват сессии** | Авто-таймаут сессии (15 мин неактивности) |
| **Блокировки Telegram API** | HTTP/SOCKS5 прокси + кастомные зеркала API |
| **Несанкционированный доступ** | `AccessControlMiddleware` — фильтрация по `ALLOWED_USER_IDS` |
| **Утечки через логи** | Чувствительные данные не попадают в логгер |
| **Утечки через чат** | Сообщения с секретами удаляются сразу после отправки |
| **SQL-инъекции** | Параметризованные запросы (`aiosqlite`) |

---

## 📋 Рекомендации пользователям

1. **Сильный мастер-пароль** — 12+ символов, буквы + цифры + спецсимволы.
2. **Блокируйте сейф** после каждого использования (`🔒 Заблокировать`).
3. **Self-host** — для максимальной безопасности запускайте бота на своём сервере.
4. **Делайте бэкапы** — экспортируйте `.snx` и храните в надёжном месте.
5. **Проверяйте код** — проект open-source именно для этого.

---

## ⚠️ Ограничения

- Seednox — **хранилище**, не кошелёк. Транзакции не подписываются.
- Telegram-боты не используют E2E шифрование. Self-hosting снижает риски.
- **Потеря мастер-пароля = безвозвратная потеря доступа** ко всем записям.

---

## 📄 Лицензия

[MIT](LICENSE) — Seednox v1.0.1
